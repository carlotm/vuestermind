{
  "packages": ["nodejs@latest"],
  "env": {
      "NODE_OPTIONS": "--openssl-legacy-provider"
  },
  "shell": {
    "scripts": {
      "deploy": "npm run build && mv dist _site",
      "test": "npm run test:unit"
    }
  }
}
